// Function taskFirst uses const
function taskFirst() {
  const task = 'I prefer const when I can.';
  return task;
}

// Function taskNext uses let
function taskNext() {
  let task = 'But sometimes let is okay.';
  return task;
}
